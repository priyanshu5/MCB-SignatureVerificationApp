import { SignatureVerificationData } from './signature-verification-data';

describe('SignatureVerificationData', () => {
  it('should create an instance', () => {
    expect(new SignatureVerificationData()).toBeTruthy();
  });
});
